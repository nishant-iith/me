# Wrangler configuration for blog proxy worker
name = "blog-proxy"
main = "worker/blog-proxy.js"
compatibility_date = "2024-01-01"

# The Hashnode API token is stored as a secret (not in this file)
# Set it with: npx wrangler secret put HASHNODE_TOKEN --config wrangler-blog-proxy.toml
# Then paste the token value when prompted
