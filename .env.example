# Environment Variables Template
# No client-side environment variables are needed.
#
# Server-side secrets (Cloudflare Workers):
# - HASHNODE_TOKEN: Set via `npx wrangler secret put HASHNODE_TOKEN --config wrangler-blog-proxy.toml`
